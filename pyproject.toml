[project]
name = "information-retrieval"
version = "0.1.0"
description = "Repo to test Information Retrieval algorithms, following USP classes."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nltk>=3.10.3",
    "pytest>=9.1.1",
]
